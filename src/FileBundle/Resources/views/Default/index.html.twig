{% extends 'base.html.twig' %}
{% block body %}
    {% include 'menu.html.twig' %}
    <section id="main-content">
        <section class="wrapper" style="margin-top: 0px;">
            <h3><i class="fa fa-angle-right"></i> Adding a new file</h3>
            <div class="row mt">
                <div class="col-lg-12">
                    <div class="form-panel">
                            {{ form_start(form, { 'attr' : { 'class': 'form-horizontal style-form' } }) }}

                            <div class="form-group">
                                <label class="col-sm-2 col-sm-2 control-label">Name</label>
                                <div class="col-sm-10">
                                    {{ form_row(form.name, { 'attr' : { 'class': 'form-control round-form' } }) }}
                                </div>
                            </div>
                            <div class="form-group">
                                <label class="col-sm-2 col-sm-2 control-label">File</label>
                                <div class="col-sm-10">
                                    {{ form_row(form.file) }}
                                </div>
                            </div>

                                <hr>
                        {{ form_row(form.sourceLanguage, { 'attr' : { 'class': 'form-control ' } }) }}

                                <br>
                        {{ form_row(form.targetLanguages, { 'attr' : { 'class': 'form-control' } }) }}

                        <br><br>
                        {{ form_row(form.save, { 'attr' : { 'class': 'btn btn-theme' } }) }}

                            {{ form_end(form) }}
                    </div>
                </div><!-- col-lg-12-->
            </div>
        </section><! --/wrapper -->
    </section> <! MENU -->

{#{{ form_start(form) }}#}
    {#{{ form_row(form.name) }}#}
    {#{{ form_row(form.file) }}#}
    {#{{ form_row(form.sourceLanguage) }}#}
    {#{{ form_row(form.targetLanguages) }}#}
{#{{ form_end(form) }}#}

{% endblock %}